import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ThisruptCode.AcademicCatalogSystem',
  appName: 'ACADEMIC CATALOG SYSTEM',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
