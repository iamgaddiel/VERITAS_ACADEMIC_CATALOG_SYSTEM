import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ACADEMIC CATALOG SYSTEM',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
